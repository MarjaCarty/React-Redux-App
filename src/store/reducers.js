const initialState = {
  isLoading: false,
  pokemonData: {},
  pokemonName: "",
  error: "",
};

export const pokeReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
